import React from "react";
import { HeroWrapper } from "./styles";

const Hero = () => (
  <HeroWrapper>
    <h1>Seja bem vindo ao painel de Desenvolvedor!</h1>
  </HeroWrapper>
);

export default Hero;
