import React from "react";
import { UserHeader, Layout } from "../../components/index";

const UserView = () => (
  <Layout pageTitle="Utilizador">
    <UserHeader></UserHeader>
  </Layout>
);

export default UserView;
